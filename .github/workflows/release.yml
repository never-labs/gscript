name: Release

on:
  push:
    tags:
      - v*
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  validate:
    runs-on: ubuntu-latest
    env:
      LEIA_CONFORMANCE_CHECK_JIT: "1"
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: tools/tree-sitter-leia/package-lock.json
      - if: github.ref_type == 'tag'
        run: |
          if ! [[ "${GITHUB_REF_NAME}" =~ ^v[0-9]+\.[0-9]+\.[0-9]+(-[0-9A-Za-z.-]+)?$ ]]; then
            echo "release tags must match vMAJOR.MINOR.PATCH or vMAJOR.MINOR.PATCH-prerelease: ${GITHUB_REF_NAME}" >&2
            exit 1
          fi
      - run: sudo apt-get update && sudo apt-get install -y luajit
      - run: scripts/install_lua_oracle.sh --bin-dir "$RUNNER_TEMP/lua/bin"
      - run: echo "LUA_BIN=$RUNNER_TEMP/lua/bin/lua" >> "$GITHUB_ENV"
      - run: npm ci --prefix tools/tree-sitter-leia
      - run: go install github.com/goreleaser/goreleaser/v2@v2.16.0
      - run: '"$(go env GOPATH)/bin/goreleaser" --version'
      - run: |
          if [ "${GITHUB_REF_TYPE}" = "tag" ]; then
            export LEIA_RELEASE_REQUIRE_TAG=1
            export LEIA_RELEASE_ARTIFACT_VERSION="${GITHUB_REF_NAME}"
            go run ./cmd/leia ci release --release-version "${GITHUB_REF_NAME}"
          else
            go run ./cmd/leia ci release
          fi

  release:
    needs: validate
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      - run: go install github.com/goreleaser/goreleaser/v2@v2.16.0
      - run: '"$(go env GOPATH)/bin/goreleaser" --version'
      - if: github.ref_type == 'tag'
        run: scripts/run.sh release-notes --require-ready --version "${GITHUB_REF_NAME}"
      - if: github.event_name == 'workflow_dispatch'
        run: '"$(go env GOPATH)/bin/goreleaser" release --snapshot --clean --skip=publish'
      - if: github.event_name == 'workflow_dispatch'
        run: scripts/run.sh release-snapshot --dist-dir dist --bin-dir /tmp/leia-snapshot-bin
      - if: github.event_name == 'push'
        run: '"$(go env GOPATH)/bin/goreleaser" release --clean --release-notes "docs/release/notes/${GITHUB_REF_NAME}.md"'
