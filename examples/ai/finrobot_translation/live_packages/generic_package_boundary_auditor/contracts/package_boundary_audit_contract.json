{
  "schema_version": 1,
  "id": "generic-package-boundary-audit-contract",
  "provider_free": true,
  "live_network": false,
  "real_dependency_imports": false,
  "audit_surfaces": [
    "package_manifest",
    "fixture_index",
    "example_registry",
    "capability_policy",
    "audit_findings",
    "missing_boundary_records"
  ],
  "required_manifest_fields": [
    "id",
    "package_name",
    "provider_free",
    "live_network_default",
    "real_dependency_import_default",
    "entrypoints",
    "schemas",
    "fixtures",
    "no_built_in_guarantee"
  ],
  "required_fixture_index_fields": [
    "fixture_key",
    "capability",
    "path",
    "schema",
    "metadata.replay_ready"
  ],
  "required_registry_fields": [
    "package_name",
    "alias",
    "registered_example",
    "provider_free",
    "capability_prefix"
  ],
  "required_missing_boundary_fields": [
    "package_id",
    "missing_boundary_type",
    "required_artifact",
    "blocking",
    "remediation"
  ],
  "acceptance_gates": [
    "manifest audit flags missing provider-free defaults and missing no-built-in guarantee",
    "fixture index audit flags missing schemas, missing files, and non replay-ready fixture metadata",
    "example registry audit flags alias drift between generic.ai.package.boundary.audit and ai.package.audit",
    "capability policy audit flags live network, provider credential, and provider SDK import allowances",
    "missing boundary records are emitted when a package lacks manifest, fixture index, registry, capability, finding, or boundary artifacts"
  ]
}
