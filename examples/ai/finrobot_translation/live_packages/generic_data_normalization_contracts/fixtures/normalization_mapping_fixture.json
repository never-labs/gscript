{
  "provider_free": true,
  "live_network": false,
  "fixture_key": "normalization_mapping:observations:offline:v1",
  "mapping_id": "mapping-observations-v1",
  "dataset_id": "dataset-observations",
  "canonical_fields": [
    {
      "canonical_field": "subject_id",
      "raw_path": "entity.id",
      "type": "string",
      "required": true,
      "nullable": false,
      "coercion": "string"
    },
    {
      "canonical_field": "period_key",
      "raw_path": "observation.period",
      "type": "string",
      "required": true,
      "nullable": false,
      "coercion": "string"
    },
    {
      "canonical_field": "measure_value",
      "raw_path": "observation.measure.raw_value",
      "type": "number",
      "required": false,
      "nullable": true,
      "coercion": "string_to_number",
      "unit_transform": "scale_basis_points_to_ratio"
    },
    {
      "canonical_field": "baseline_value",
      "raw_path": "observation.baseline.raw_value",
      "type": "number",
      "required": false,
      "nullable": true,
      "coercion": "integer_to_number",
      "unit_transform": "identity"
    }
  ],
  "field_policies": [
    {
      "canonical_field": "subject_id",
      "missing_policy": "reject_record",
      "stale_policy": "not_applicable",
      "quality_check": "non_empty_string"
    },
    {
      "canonical_field": "measure_value",
      "missing_policy": "emit_null_with_reason",
      "stale_policy": "warn_when_observed_after_stale_after",
      "quality_check": "nullable_number"
    },
    {
      "canonical_field": "baseline_value",
      "missing_policy": "emit_null_with_reason",
      "stale_policy": "warn_when_observed_after_stale_after",
      "quality_check": "nullable_number"
    }
  ],
  "type_coercions": [
    {"id": "string", "from": "any", "to": "string", "lossless": true},
    {"id": "string_to_number", "from": "string", "to": "number", "lossless": false},
    {"id": "integer_to_number", "from": "integer", "to": "number", "lossless": true}
  ],
  "unit_transforms": [
    {"id": "identity", "from_unit": "unit", "to_unit": "unit", "scale": 1.0},
    {"id": "scale_basis_points_to_ratio", "from_unit": "basis_points", "to_unit": "ratio", "scale": 0.0001}
  ],
  "deterministic_order": [
    "dataset_id",
    "subject_id",
    "period_key",
    "source_row"
  ]
}
