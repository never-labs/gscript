{
  "schema_version": 1,
  "id": "finrobot-retail-sentiment-adapter-clean-skip-contract",
  "provider_free": true,
  "live_network": false,
  "real_dependency_imports": false,
  "clean_skip_without_dependency": true,
  "skip_schema": "schemas/adapter_clean_skip_v1.schema.json",
  "skip_fixture": "fixtures/adapter_clean_skip_fixture.json",
  "optional_adapters": [
    "adanos",
    "reddit",
    "x_social",
    "polymarket"
  ],
  "fallback_rules": [
    "return ok=false with skip=true when an optional adapter dependency is unavailable",
    "do not import q/runtime, provider SDKs, requests, http clients, praw, tweepy, xai, polymarket clients, or adanos clients",
    "preserve adapter_id, requested_capability, provider_free, live_network=false, and real_dependency_imports=false in the skip envelope",
    "include deterministic fixture_key and human-readable unsupported_reason"
  ],
  "acceptance_gates": [
    "optional adapter clean-skip is fixture-backed",
    "skip envelope never requires credentials or network access",
    "dependency_imported is false for all optional retail sentiment adapters"
  ]
}
