row := {id: "advanced-agent-fingpt-forecaster" provider_free: true live_network: false credentials_required: false artifacts: {"forecast_section_schema", "market_news_fixture_manifest", "optional_model_gate_record"}}
assert(row.provider_free == true)
assert(row.live_network == false)
assert(row.credentials_required == false)
assert(#row.artifacts == 3)
print("tutorial_parity_matrix row=advanced-agent-fingpt-forecaster artifacts=3 provider_free=true live_network=false credentials_required=false")
