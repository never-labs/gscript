print("case:nextvar_ipairs_protocol_more")

x := 0
t := [10, 20, 30]
t.x = 12
for k, v := range ipairs(t) {
  x = x + 1
  assert(k == x && v == x * 10)
}

for _ := range ipairs({x: 12, y: 24}) {
  assert(nil)
}

assert(type(ipairs([])) == "function" && ipairs([]) == ipairs([]))

print("ok")
