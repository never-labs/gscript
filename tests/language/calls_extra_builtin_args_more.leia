print("case:calls_extra_builtin_args_more")

rawget({}, "x", 1)
rawset({}, "x", 1, 2)
assert(math.sin(1, 2) == math.sin(1))

t := [10, 9, 8, 4, 19, 23, 0, 0]
comp := func(a, b) {
  return a < b
}
table.sort(t, comp, "extra arg")
for i := 2; i <= #t; i++ {
  assert(t[i - 1] <= t[i])
}

print("ok")
